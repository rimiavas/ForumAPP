<!DOCTYPE html>
<html>
<head>
    <title>List Of All Posts</title>
    <link rel="stylesheet"  type="text/css" href="/main.css" />
</head>

<body>

    <h1 class="page-logo">
        <a href= "home"><%=forumName%>
            <p>!</p>
            <h2>Forum website</h2>
        </a>
    </h1>

    <h2 class="page-title"> All <%=forumName%> <span>!</span> Posts </h2>
    
    <main>
        <div class="posts-container">
            <% for (let i = 0; i < allPosts.length; i++) { %>
                <!-- Display list of all user's name and description -->
                <div class="topic-label-box"><%= allPosts[i].topic%></div>
                <div class="outer-box">
                    <h1><%= allPosts[i].title%> </h1>
                    <p1>by <%= allPosts[i].creator%>  at <%= allPosts[i].created_at%></p1> <br><br>
                    <div class="main-content"><%= allPosts[i].content%></div>
                    <form class="delete-btn"action="/delete-post" method="post">
                        <input type="hidden" name="postId" value="<%= allPosts[i].id %>">
                        <button type="submit">Delete</button>
                    </form>
                </div>
            <% } %>
        </div>
    </main>
</body>

</html>
